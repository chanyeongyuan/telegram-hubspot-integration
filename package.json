{
  "name": "telegram-hubspot-integration",
  "version": "0.1.0",
  "private": true,
  "description": "Bridge Telegram Business 1:1 enquiries into HubSpot custom channels.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check scripts/register-hubspot-channel.js && node --check scripts/create-channel-account.js && node --check scripts/set-telegram-webhook.js && npm run check:hubspot-project",
    "check:hubspot-project": "node -e \"const fs=require('node:fs'); for (const file of ['hsproject.json','hubspot/app/app-hsmeta.json']) JSON.parse(fs.readFileSync(file,'utf8'));\"",
    "hubspot:project:validate": "hs project validate",
    "hubspot:project:upload": "hs project upload",
    "hubspot:project:open": "hs project open",
    "setup:hubspot-channel": "node scripts/register-hubspot-channel.js",
    "setup:hubspot-account": "node scripts/create-channel-account.js",
    "setup:telegram-webhook": "node scripts/set-telegram-webhook.js"
  }
}
